import UIKit
import EMTNeumorphicView

class AppLayout {
    
    static let alignment: UIStackView.Alignment = UIStackView.Alignment.center
    static let distribution: UIStackView.Distribution = UIStackView.Distribution.fillEqually
    static let spacing: CGFloat = 20
    static let cornerRadius: CGFloat = 20
    static let elementDepth: CGFloat = 100
    static let depthType: EMTNeumorphicLayerDepthType = .convex
}
